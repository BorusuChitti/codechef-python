
'''Collatz sequence'''
from collections import Counter

def find_duplicates(arr):
    return [item for item, count in Counter(arr).items() if count > 1]

# Example usage
arr = [1, 2, 3, 4, 5, 3, 2]
print("Duplicates:", find_duplicates(arr))  # Output: [3, 2]
